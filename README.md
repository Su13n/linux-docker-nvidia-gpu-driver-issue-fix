# linux-docker-nvidia-gpu-driver-issue-fix
If your Immich containers fail to start with errors like:  ```text could not select device driver "nvidia" with capabilities: [[gpu]] ```  or  ```text could not select device driver "nvidia" with capabilities: [[gpu compute video]] ```
